# Sample outputs (canonical run)

Moved to [`examples/`](../examples/README.md) — the agent's investigation answer, the resolution-log excerpt, and `--verify-only` output, split into individual files and indexed there.
